{
  "name": "totem-smart-contracts",
  "version": "1.0.0",
  "description": "Totem GDN Smart Contracts",
  "engines": {
    "node": "^18.0.0"
  },
  "main": "index.js",
  "scripts": {
    "prettier": "prettier --write 'contracts/**/*.sol'",
    "local-node": "hardhat node",
    "compile": "hardhat compile",
    "deploy": "hardhat deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.3"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.1.1",
    "dotenv": "^16.0.1",
    "ethers": "^5.6.9",
    "hardhat": "^2.10.2",
    "hardhat-abi-exporter": "^2.10.0",
    "prettier": "^2.7.1",
    "prettier-plugin-solidity": "^1.0.0-beta.24"
  },
  "prettier": {
    "overrides": [
      {
        "files": "*.sol",
        "options": {
          "printWidth": 120,
          "compiler": "0.8.9"
        }
      }
    ]
  }
}